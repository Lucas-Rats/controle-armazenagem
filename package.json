{
  "name": "controle-armazenagem",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:schema": "wrangler d1 execute armazenagem-db --remote --file=./schema.sql",
    "db:schema:local": "wrangler d1 execute armazenagem-db --local --file=./schema.sql"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
